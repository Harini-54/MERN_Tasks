
Variables Declarations

1. Create variables:
   - Declare variables `firstName`, `lastName`, and `age` using `var`, `let`, and `const` keywords.
   - Assign appropriate values to each variable.
   - Print the values of these variables to the console.

2. Variable scope:
   - Demonstrate the difference between global and local variable scopes using nested functions.
   - Explain the implications of using `var` versus `let` and `const` in terms of scope.

